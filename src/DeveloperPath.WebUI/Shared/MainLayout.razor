
@inherits LayoutComponentBase
@inject IStringLocalizer<LanguageResources> localizer
 
<MudLayout>
    <MudAppBar Elevation="4" Color="Color.Primary" Class="main-appbar">
        <a href="/">
          <MudIcon Icon="@DeveloperPathIcon.DeveloperPath" Class="top-logo" Size="Size.Large" ViewBox="0, 0, 400,400"></MudIcon>
        </a>
        <MudHidden Breakpoint="Breakpoint.Xs">
            <MudText Typo="Typo.h3" Class="title">
              <a href="/">@localizer["DeveloperPath"]</a>
            </MudText>
        </MudHidden>
        <MudToolBar DisableGutters="true">
            <MudIconButton Icon="@Icons.Material.Outlined.Menu" Color="Color.Inherit" OnClick="DrawerToggle" />
        </MudToolBar>
        <MudSpacer />
        <CultureSelector></CultureSelector>
        <ThemeSelector></ThemeSelector>
        <LoginDisplay></LoginDisplay>
    </MudAppBar>
    <MudDrawer @bind-open="DrawerOpened" Elevation="25" ClipMode="DrawerClipMode.Always">
        <NavMenu/>
    </MudDrawer>
    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.False" Class="mt-4">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>


@code
{
    bool DrawerOpened = true;

    void DrawerToggle()
    {
        DrawerOpened = !DrawerOpened;
        StateHasChanged();
    }
}